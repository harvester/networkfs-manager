name: Dev Build and Publish

on:
  push:
    branches:
    - main
    - 'v1.*'

jobs:
  build-for-dev:
    uses: ./.github/workflows/factory.yml
    with:
      tag: ${{ github.ref_name }}-head
      push: true
    secrets: inherit